/**
 * Type declaration for the pipeline-generated profile-data.js.
 * The file itself stays as plain JS (auto-written by scripts/update-resume.js);
 * this .d.ts tells TypeScript what shape to expect when modules import it.
 */
import type { ProfileData } from './js/modules/types.js';

export declare const profileData: ProfileData;
